{% set version = "1.8.7" %}

package:
    name: libfind
    version: {{ version }}

source:
  path: ..

build:
    number: 0
    noarch: generic

requirements:
    run:
        - cmake >=3.12

about:
    summary: A collection of cmake helpers for SCNS
    license_family: MIT
